const mysql = require('mysql2')

const conn = mysql.createConnections({
    host : 'localhost',
    user : 'root',
    password : '1234',
    port : 3006,
    database: ''
})

conn.connect();

module.exports = conn;